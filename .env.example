# Optional. Sets MyMemory daily quota to 50 000 chars (vs 5 000 anonymous).
# Use any contact address — MyMemory only uses it for rate-limit attribution.
VITE_TRANSLATION_EMAIL=

# Set in production (auto-written by scripts/deploy.mjs after Worker deploy).
# Leave empty for local dev — Vite proxies /api/* to localhost:8787.
VITE_API_BASE=

# Canonical public URL of the deployed frontend. Used for og:url meta,
# share URLs, exported pack footers, and the data-wipe instruction in
# /privacy. The committed .env file holds the staging default
# (vozclara.pages.dev); deploy.mjs rewrites this in .env.production
# from the active [[routes]] pattern in worker/wrangler.toml.
VITE_SITE_URL=

# Cloudflare Web Analytics beacon token. Get it from dash.cloudflare.com
# → Analytics & Logs → Web Analytics → Add a site → vozclara.app, then
# paste the JS token here. Absent = beacon skipped, no client-side
# metrics (Cloudflare still aggregates server-side traffic in the Pages
# project dashboard automatically).
VITE_CF_BEACON_TOKEN=

# Plausible Analytics domain — cookieless funnel-event tracking on top
# of the Cloudflare Web Analytics baseline. Activates the snippet +
# `track()` calls in `src/lib/analytics.ts`. Absent = analytics calls
# silently no-op so the call sites stay in place before activation.
# Setup: create the site on plausible.io with the canonical apex domain
# (vozclara.app), then paste that same domain here. Configure goal
# names per the funnel taxonomy in analytics.ts.
VITE_PLAUSIBLE_DOMAIN=

# Sentry DSN for production error tracking. Create a project at
# sentry.io (React + Browser), copy the DSN. Absent = errors stay
# on console + ErrorBoundary fallback, no external upload.
VITE_SENTRY_DSN=

# Founder Deal Stripe Payment Link. Create the product in Stripe
# (€99 one-time, EUR), generate a Payment Link, paste the
# https://buy.stripe.com/... URL here. Absent / "PLACEHOLDER" makes
# the /founder page render the "Live in hours" placeholder instead.
VITE_FOUNDER_CHECKOUT_URL=
