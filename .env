# Loaded by Vite for every mode (dev + prod) as the base default. The
# production-specific values get written into .env.production by
# scripts/deploy.mjs and override these.
VITE_SITE_URL=https://vozclara.pages.dev
